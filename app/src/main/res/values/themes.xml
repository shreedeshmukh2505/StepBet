<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.StepBet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary Colors -->
        <item name="colorPrimary">@color/primary_400</item>
        <item name="colorPrimaryVariant">@color/primary_600</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary Colors -->
        <item name="colorSecondary">@color/green_400</item>
        <item name="colorSecondaryVariant">@color/green_600</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background Colors for Dark Theme -->
        <item name="android:colorBackground">@color/gray_900</item>
        <item name="colorOnBackground">@color/white</item>

        <!-- Surface Colors -->
        <item name="colorSurface">@color/gray_800</item>
        <item name="colorOnSurface">@color/white</item>

        <!-- Error Colors -->
        <item name="colorError">@color/red_400</item>
        <item name="colorOnError">@color/white</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/gray_900</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/gray_900</item>

        <!-- Enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>

        <!-- Text Colors -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/gray_300</item>
        <item name="android:textColorTertiary">@color/gray_400</item>
    </style>

    <style name="Theme.StepBet" parent="Base.Theme.StepBet" />

    <!-- Dark Card Style -->
    <style name="DarkCardViewStyle" parent="CardView">
        <item name="cardBackgroundColor">@color/gray_800</item>
        <item name="cardElevation">8dp</item>
        <item name="cardCornerRadius">16dp</item>
    </style>

    <!-- Dark Button Style -->
    <style name="DarkButtonStyle" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/primary_500</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>